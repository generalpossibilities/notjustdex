name: notjustdex_chat_relay
description: Decentralized chat relay server — anyone can run one
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shelf: ^1.4.2
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^3.0.1
  crypto: ^3.0.3
  args: ^2.6.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.8
