# Feature flags — every module independently togglable.
# No Go service hosts needed; all features are decentralized.

feed:
  enabled: true

chat:
  enabled: true

mini_apps:
  enabled: true

notifications:
  enabled: true

creator_economy:
  enabled: true

search:
  enabled: true

moderation:
  enabled: true

media:
  enabled: true

analytics:
  enabled: true

dao:
  enabled: false

vault:
  enabled: true
