name: dexchats_in_app_browser
description: In-app browser for DexChats with WebView, nav controls, deep links.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  webview_flutter_android: ^4.1.0
  webview_flutter_wkwebview: ^3.16.0
  url_launcher: ^6.3.0
