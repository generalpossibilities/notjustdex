name: d_vault
version: 1.0.0
description: Decentralized vault module for NotJustDex (password & seed storage on Acki Nacki)

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cryptography: ^2.9.0
  bip39: ^1.0.6
  http: ^1.6.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.8
