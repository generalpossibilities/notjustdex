# NotJustDex

project built on Acki Nacki.

Monorepo with Flutter frontends and Go backends, deployed as microservices.

Blockchain layer uses TVM Solidity smart contracts with a Dapp ID root contract pattern.

Identity system is on-chain. Everything else is modular and optional.

will continue to envolve.
